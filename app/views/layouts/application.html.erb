<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Tracker</title>
    <!-- Bootstrap CSS -->
  <%= stylesheet_link_tag "bootstrap.min", "data-turbo-track": "reload" %>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %> <!-- これが importmap のカギ -->


  <!-- Bootstrap JS（ローカル）-->
  <%= javascript_include_tag "bootstrap.bundle.min", "data-turbo-track": "reload", defer: true %>

  
</head>
<body class="bg-body-tertiary" style="padding-top: 70px;">
  <%= render 'layouts/header' %>
  <div class="container">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
  <%= yield %>
  <%= render 'layouts/footer' %>   
  <%= debug(params) if Rails.env.development? %> 
  </div>
</body>
</html>